# Learning

这里保存围绕单个概念的小型练习。按实际学习进度创建主题目录，例如
`python/`、`math/`、`pytorch/`、`audio/`、`music-theory/` 和
`transformers/`。

适合放在这里的内容：

- 一个公式或算法的最小实现；
- 一个 PyTorch API 的行为验证；
- 短小、确定、可以快速运行的练习；
- 与练习紧邻的测试和简要说明。

需要交互式探索时放入 `notebooks/`；需要完整输入输出、配置、评测和报告时放入
`projects/`。
